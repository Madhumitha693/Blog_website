<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    
    <title>Blog website</title>

    <style>
        /* Background image for the entire body */
        body {
            background-image: url('/images/bg1.jpg'); /* Replace with your image URL */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: white;
            font-size: large;
        }

        /* Add a semi-transparent background for the container */
        .container {
            padding: 20px;
            border-radius: 10px;
        }

        h1 {
            color: #ffffff; /* Text color */
            text-align: center; 
            font-family: Georgia, 'Times New Roman', Times, serif; /* Font family */
            font-size: 3rem; /* Increased font size */
            margin-bottom: 1.5rem; /* Spacing below the heading */
            font-weight: bold; /* Bold text */
            text-transform: uppercase; /* Uppercase letters */
            letter-spacing: 2px; /* Spacing between letters */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3); /* Subtle shadow for depth */
            padding: 10px 0; /* Vertical padding */
            border-bottom: 3px solid rgba(255, 98, 0, 0.5); /* Underline with a soft border */
            background-color: rgba(255, 255, 255, 0.2); /* Light background for contrast */
            border-radius: 10px; /* Rounded corners */
            transition: color 0.3s ease, transform 0.3s ease; /* Smooth transition for color and scale */
        }

        h1:hover {
            color: #ff98c8; /* Change color on hover */
            transform: scale(1.05); /* Slightly enlarge on hover */
        }

        /* Styling the card */
        .card { /* Light background for cards */
            color: black;
            border-radius: 10px;
        }

        .card-title {
            font-weight: bold;
            color: #333;
        }

        .btn {
            margin-right: 10px;
        }

        /* Styling the buttons */
        .btn-success, .btn-primary, .btn-info, .btn-danger {
            border-radius: 5px;
            padding: 10px 20px;
        }

        /* Add spacing between the cards */
        .card + .card {
            margin-top: 20px;
        }

        .btn-success {
            background-color: #42214a; /* Default green background */
            color: white; /* Text color */
            text-decoration: none;
            border: none; /* Remove border */
            border-radius: 5px; /* Rounded corners */
            padding: 12px 20px; /* Padding for better click area */
            font-weight: bold; /* Bold text */
            font-size: 1rem; /* Font size */
            transition: background-color 0.3s, transform 0.2s; /* Smooth transition */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Subtle shadow for depth */
        }

        .btn-success:hover {
            background-color: #efbeeb; /* Darker green on hover */
            transform: translateY(-2px); /* Slight lift effect on hover */
        }

        .btn-success:active {
            background-color: #c487c1; /* Even darker green when clicked */
            transform: translateY(0); /* Reset the lift effect */
        }

        .btn-success:focus {
            outline: none; /* Remove outline on focus */
            box-shadow: 0 0 0 0.2rem rgb(205, 132, 205); /* Focus ring */
        }

        .btn-primary {
            background-color: #6c2973; 
            text-decoration: none;/* Default Bootstrap primary blue background */
            color: white; /* Text color */
            border: none; /* Remove border */
            border-radius: 5px; /* Rounded corners */
            padding: 12px 20px; /* Padding for better click area */
            font-weight: bold; /* Bold text */
            font-size: 1rem; /* Font size */
            transition: background-color 0.3s, transform 0.2s; /* Smooth transition */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Subtle shadow for depth */
        }

        .btn-primary:hover {
            background-color: #eebef0; /* Darker blue on hover */
            transform: translateY(-2px); /* Slight lift effect on hover */
        }

        .btn-primary:active {
            background-color: #d3a1d4; /* Even darker blue when clicked */
            transform: translateY(0); /* Reset the lift effect */
        }

        .btn-primary:focus {
            outline: none; /* Remove outline on focus */
            box-shadow: 0 0 0 0.2rem rgb(187, 116, 176); /* Focus ring */
        }

        .desc {
            background-color: rgba(255, 255, 255, 0.1); /* Light transparent background */
            padding: 20px; /* Padding around the content */
            border-radius: 10px; /* Rounded corners */
            margin-top: 20px; /* Space above each description */
            transition: background-color 0.3s ease, transform 0.3s ease; /* Smooth transition for hover effects */
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Subtle shadow for depth */
        }

        .desc:hover {
            background-color: rgba(255, 255, 255, 0.29); /* Darker background on hover */
            transform: translateY(-2px); /* Slight lift effect on hover */
        }

        .card-title {
            color: white;
        }

        .card-subtitle {
            margin-bottom: 20px; /* Adjust this value to increase or decrease space below the date */
        }

        .card-text {
            margin-top: 20px; /* Adjust this value to increase or decrease space above the description */
        }

        .card-subtitle {
            margin-top: 20px; /* Adjust this value to increase or decrease space above the date */
            margin-bottom: 10px; /* Space below the date */
        }

        /* Styling for the search bar */
        .search-bar {
            margin: 20px 0; /* Spacing above and below the search bar */
        }

        .search-bar input {
            border-radius: 5px 0 0 5px; /* Rounded left corners */
        }

        .search-bar button {
            border-radius: 0 5px 5px 0; /* Rounded right corners */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Blog Website</h1>

        <!-- Search Bar -->
        <form class="search-bar d-flex" action="/" method="GET">
            <input type="text" class="form-control" placeholder="Search articles..." aria-label="Search" name="query" required>
            <button type="submit" class="btn btn-success">Search</button>
        </form>

        <a href="/articles/new" class="btn-success">Create Article</a>
        <% articles.forEach(article => { %>
            <div class="desc">
                <div class="card-body">
                    <h4 class="card-title"><%= article.title %></h4>
                    <div class="card-subtitle">
                        <%= article.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2">
                        <%= article.description %>
                    </div>
                    <a href="articles/<%= article.slug %>" class="btn-success">Read More</a>
                    <a href="articles/edit/<%= article.id %>" class="btn-primary">Edit</a>
                    <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn-success">Delete</button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>
